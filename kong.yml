# kong.yml
# This is a declarative configuration file for Kong.
# It defines the services, routes, and plugins for the API gateway.

_format_version: "3.0"

# Define a service that points to our Next.js application.
services:
  - name: nextjs-app-service
    url: http://app:3000
    routes:
      # Public authentication routes (only login/register allowed without JWT)
      - name: public-auth-routes
        paths:
          - /api/auth/login
          - /api/auth/register
          - /api/auth/logout
        methods:
          - POST
        strip_path: false
      # Public health check (for monitoring)
      - name: public-health-routes
        paths:
          - /api/health
          - /api/debug
        methods:
          - GET
        strip_path: false
      # Static assets (CSS, JS, images) - needed for login page
      - name: public-static-routes
        paths:
          - /_next
          - /favicon.ico
          - /static
        methods:
          - GET
        strip_path: false
      # Login page route (public access to show login form)
      - name: public-login-page
        paths:
          - /login
        methods:
          - GET
        strip_path: false
      # ALL OTHER ROUTES - Require JWT authentication
      - name: protected-all-routes
        paths:
          - /
        methods:
          - GET
          - POST
          - PUT
          - DELETE
          - PATCH
        strip_path: false

# JWT Consumers (users who can authenticate)
consumers:
  - username: audit-app
    custom_id: audit-app-consumer
    jwt_secrets:
      - key: audit-app-key
        secret: your-256-bit-secret-key-here-change-this-in-production

# Security and rate limiting plugins
plugins:
  # JWT Authentication - applied to ALL protected routes (everything except login/register/health/static)
  - name: jwt
    route: protected-all-routes
    config:
      key_claim_name: iss
      secret_is_base64: false
      anonymous: null  # No anonymous access allowed
      
  # Request Termination for unauthorized access (optional - adds extra security)
  - name: request-termination
    route: protected-all-routes
    config:
      status_code: 401
      message: "Authentication required. Please login first."
    enabled: false  # Only enable if JWT fails
      
  # Rate limiting to prevent abuse
  - name: rate-limiting
    service: nextjs-app-service
    config:
      minute: 100
      hour: 1000
      policy: local
      
  # Request size limiting
  - name: request-size-limiting
    service: nextjs-app-service
    config:
      allowed_payload_size: 10
      
  # CORS for web applications
  - name: cors
    service: nextjs-app-service
    config:
      origins:
        - "*"
      methods:
        - GET
        - POST
        - PUT
        - DELETE
        - PATCH
        - OPTIONS
      headers:
        - Accept
        - Accept-Version
        - Content-Length
        - Content-MD5
        - Content-Type
        - Date
        - Authorization
      exposed_headers:
        - X-Auth-Token
        - Authorization
      credentials: true
      max_age: 3600
      
  # Security headers
  - name: response-transformer
    service: nextjs-app-service
    config:
      add:
        headers:
          - "X-Frame-Options: DENY"
          - "X-Content-Type-Options: nosniff"
          - "X-XSS-Protection: 1; mode=block"
          - "Strict-Transport-Security: max-age=31536000; includeSubDomains"
          
  # HTTP Log plugin to send logs to Fluent Bit
  - name: http-log
    service: nextjs-app-service
    config:
      http_endpoint: http://fluent-bit:24224/kong
      method: POST
      timeout: 10000
      keepalive: 60000
      
  # File Log plugin for local logging
  - name: file-log
    service: nextjs-app-service
    config:
      path: /tmp/kong_access.log
      
  # Prometheus plugin for metrics
  - name: prometheus
    service: nextjs-app-service
    config:
      per_consumer: true
